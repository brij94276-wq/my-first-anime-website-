# Anime Website

Static anime website ready for deployment on Railway.

## Run locally

```bash
npm install
npm start
```

Then open http://localhost:3000.

## Railway

Railway can deploy this project as a Node.js app using `npm start`.

If you connect this repository to Railway, it will detect the Node.js app and deploy automatically.
